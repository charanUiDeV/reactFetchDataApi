import * as React from 'react';

export const About: React.StatelessComponent<{}> =()=>{
    return(
        <div className="container"> 
            <h1>React- routing</h1>
            <p>this application fetches and displays the data from an API to the user</p>
        </div>
    )
}
